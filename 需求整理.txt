1.模块按需加载，发布时自动压缩，并且可以有debug模式
2.模块定义格式不能太复杂，最好自动生成
3.发布模块不能太麻烦，最好有个引导机制，命名不能没有规范，要定义一个让人根据规范就能马上给该模块起个唯一且分类明确的ID的模块命名规范，以便线上预览，检查，线下安装时比较方便
4.安装模块时响应不能太慢，以免影响使用者的心情
5.模块文档编写不能太复杂，最好可以根据注释自动生成，没有写注释的时候有个默认格式，并且也提供文档生成，事例展示，以便后续发现完善
6.远端模块预览有个友好界面，和方便的检索、总览机制，并且响应速度不能太慢（至少要比github那个速度快才行）
7.

